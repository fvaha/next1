// src/locales/types.ts

import en from "./en";

// This type always matches your en.ts structure.
// All translation files must have the same structure as en.ts.
export type Translation = typeof en;
